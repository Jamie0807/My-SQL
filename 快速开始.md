# 快速开始指南

## 🎉 恭喜！数据库已创建成功

你的 SQLite 数据库 `practice.db` 已经创建完成，包含完整的示例数据。

## 📖 三种使用方式

### 方式1：在 VS Code 中使用 Database Client（推荐）⭐

1. **打开 Database Client**
   - 点击左侧活动栏的数据库图标（cylinder 图标）
   - 或者按 `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)，输入 "Database Client"

2. **连接数据库**
   - 点击 "+" 按钮或 "New Connection"
   - 选择 **SQLite**
   - 浏览并选择文件：`/Users/xiaoqianqian/Desktop/mycode/My-SQL/practice.db`
   - 点击 "Connect"

3. **开始使用**
   - 展开连接，查看所有表
   - 右键表名 → "Show Table" 查看数据
   - 右键连接 → "New Query" 执行 SQL

### 方式2：在终端中使用 SQLite

```bash
# 进入数据库
sqlite3 practice.db

# SQLite 命令提示符下，运行 SQL：
SELECT * FROM students;

# 查看所有表
.tables

# 查看表结构
.schema students

# 退出
.quit
```

**常用 SQLite 命令：**
```sql
.tables              -- 显示所有表
.schema table_name   -- 显示表结构
.mode column         -- 列模式显示
.headers on          -- 显示列名
.quit                -- 退出
```

### 方式3：使用 SQLite 扩展

在 VS Code 中：
1. 在文件浏览器中找到 `practice.db`
2. 右键点击 → "Open Database"
3. 在打开的界面中查看和编辑数据

## 🎯 学习路径

### 第一步：了解基础知识
打开 `notes/SQL基础知识.md`，阅读：
- SQL 的基本概念
- 数据类型
- SQL 语法规范

### 第二步：开始第一课
打开 `01-基础语法/01-创建数据库和表.sql`：

1. **阅读代码** - 理解每个 SQL 语句的作用
2. **运行语句** - 在 Database Client 或终端中执行
3. **做练习** - 完成文件末尾的练习题
4. **记笔记** - 在文件的"学习笔记"部分记录心得

### 第三步：按顺序学习
```
01-基础语法/
├── 01-创建数据库和表.sql  ← 从这里开始
├── 02-插入数据.sql

02-查询操作/
├── 01-基础查询.sql
├── 02-条件查询.sql
```

## 💡 学习技巧

### 1. 边学边练
```sql
-- 不要只看，要动手！
-- 例如：查询所有学生
SELECT * FROM students;

-- 查询 GPA 大于 3.8 的学生
SELECT name, gpa FROM students WHERE gpa > 3.8;

-- 查询计算机科学系的课程
SELECT * FROM courses WHERE department = 'Computer Science';
```

### 2. 完成练习题
每个练习文件末尾都有练习题，例如：

```sql
/*
练习1: 查询所有女生的姓名和邮箱
*/

-- 在这里写你的答案
SELECT name, email FROM students WHERE gender = 'Female';
```

### 3. 做实验
尝试修改查询，看看结果如何变化：
```sql
-- 原查询
SELECT * FROM students WHERE gpa > 3.5;

-- 改成
SELECT name, gpa FROM students WHERE gpa > 3.8 ORDER BY gpa DESC;

-- 再改成
SELECT name, gpa FROM students WHERE gpa BETWEEN 3.7 AND 3.9;
```

## 📊 数据库内容预览

### 学生表 (students) - 15 条记录
```
John Smith, Emma Johnson, Michael Brown, Sophia Davis...
```

### 课程表 (courses) - 15 条记录
```
Database Systems, Calculus, Data Structures, Operating Systems...
```

### 选课表 (enrollments) - 30 条记录
```
Student enrollment records with grades
```

## 🔍 快速测试查询

打开 Database Client 或终端，试试这些查询：

```sql
-- 1. 查看所有表
SELECT name FROM sqlite_master WHERE type='table';

-- 2. 查询 GPA 最高的 5 个学生
SELECT name, gpa FROM students ORDER BY gpa DESC LIMIT 5;

-- 3. 查询计算机科学系有多少门课
SELECT COUNT(*) as course_count FROM courses 
WHERE department = 'Computer Science';

-- 4. 查询每个学生的平均成绩
SELECT s.name, AVG(e.grade) as avg_grade
FROM students s
JOIN enrollments e ON s.student_id = e.student_id
GROUP BY s.student_id, s.name
ORDER BY avg_grade DESC;

-- 5. 查询成绩大于 90 分的选课记录
SELECT s.name, c.course_name, e.grade
FROM enrollments e
JOIN students s ON e.student_id = s.student_id
JOIN courses c ON e.course_id = c.course_id
WHERE e.grade > 90
ORDER BY e.grade DESC;
```

## 🆘 遇到问题？

### 常见问题

**Q: 数据库文件在哪里？**
A: `/Users/xiaoqianqian/Desktop/mycode/My-SQL/practice.db`

**Q: 如何重置数据库？**
A: 删除 `practice.db`，然后重新运行：
```bash
sqlite3 practice.db < sample-data/init-sqlite.sql
```

**Q: SQLite 和 MySQL 有什么区别？**
A: SQLite 更简单，适合学习；MySQL 适合生产环境。本项目的 `init.sql` 是 MySQL 版本，`init-sqlite.sql` 是 SQLite 版本。

**Q: 如何查看表结构？**
A: 
- 在 Database Client 中：右键表 → "Show Table Info"
- 在终端中：`.schema students`

## 📝 记录学习进度

在每个练习文件的"学习笔记"部分记录：
- ✅ 完成日期
- 🤔 遇到的问题
- 💡 解决方案
- 📌 重要知识点

## 🎓 下一步

1. ✅ 数据库已创建 ← 你已完成这一步！
2. 📖 阅读 SQL 基础知识
3. 💻 打开第一个练习文件
4. ✍️ 完成练习题
5. 🚀 继续下一课

---

**祝你学习愉快！有任何问题随时问我。** 🎉
